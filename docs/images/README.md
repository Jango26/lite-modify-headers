# Screenshots

Two PNGs, referenced by `README.md` and `README.zh-CN.md`.

| File                   | What to capture                                                                                                                                                                           |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `options-overview.png` | The whole configuration page: a few filled-in rules plus one group, switch on _Running_. This is the hero image — it should make the "one table, that's the whole app" point at a glance. |
| `popup.png`            | The toolbar popup after clicking the extension icon: the start/stop toggle, the per-rule toggles, and _Open configuration_.                                                               |
